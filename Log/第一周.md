## 周报 · web · D1 数据

1. **环境搭建耗时**： 3小时0分 （未超出预算）
2. **首次成功注入时间**： 20分钟
3. **协议映射**： 7.5分
   - 类比清晰度：在实践过程中并未刻意与内存修改做对比，而是凭借感觉尝试了一些方法，比如标准模式中提出的;ls起初只是觉得像linux中的指令，但是并不能确定真正用法，看的懂，自己做的话，还不行，因为本身对指令部分就没有熟练度，不过也在没有得到期望的反馈之后，也尝试性的做出改动，比如前面加空格，或者尝试&;ls ,&&ls ,ls ,;&dir，不知道准确用法，但是不断测试测试出来了
4. **心流强度自评**： 10分
   - 几乎全局都是专注状态，在简单问题上，比如在环境安装上，是这样，在实战阶段，在没有产生预期效果之后，这种情况多发生几次，短暂产生走神，但是很快可以再次进入状态
5. **创造冲动信号**
   - 8 我自发地想要尝试其他输入点
   - 0 我想写个脚本跑载荷
   - 9 我想立刻调高难度试试
   - 0 暂无强烈冲动
6. **最大障碍**
   - 工具配置
     - 因为并没有一开始照搬视频教程，虽然后来还是通过视频解决关键性问题（比如在burp中到底在哪个界面抓包，没有网怎么办），虽然有的问题因为时间问题还是没有解决，比如burp使用了内部浏览器而不是通过代理使用外部浏览器，这里的网络问题花费了比较长的时间，排查了虚拟机和真机之间的ping不通的问题，没有按照计划之中使用linux系统，而是用了虚拟机中的win10系统中安装phpstudy，然后再将dvwa放入其服务器目录下。
   - 其他
     - 如果算的话，我觉得我没有看到ai给出的内存与web对比那句话，我没法一开始就想到，web中的可插入点，漏洞点，对于内存中的关键地址，内存的话值本身不重要，如何找到这个地址是关键，对比web中，我觉得如何找到插入点，可以有事半功倍的效果，但是web与内存不同的是，web通过对值本身的操作，可以做到让一个包装的很好的点露出破绽，总之，值的操作与查找插入位置的方式，两者都很重要。
7. **额外补充内容**
   - xss原理（反射型，存储型，DOM）
   - xss的历史发展，发现其本质就是对代码和数据进行混淆，从历史角度看，是从html出现，到js，一步一步给自己挖坑，从简单的script利用然后到蠕虫病毒开始爆发。
   - 认识了几种xss注入的绕过方法，如scr<...>ipt,或者img或者onload

## 周报 · web · D2 数据

### 一、核心战果
1.  **反射型XSS (Medium) 攻克**：
    - 关键绕过技巧：`<script>` 被过滤后，我尝试了 `<img src=x onerror=alert(1)>_<svg onload=alert(1)>` 并成功。
    - 核心体会：难度提升主要在于___如果完全不了解反射型XSS而且不了解html标签，很难操作___，这让我感到___更有挑战性___（例如：更具挑战性/有点繁琐）。
2.  **存储型XSS (Low & Medium) 自主探索**：
    - 与反射型的核心差异认知：___存储型会存入数据库从而面向大众，需要更隐蔽的操作___。
    - 从发现到利用的路径：___low的话，尝试了和反射型一样的方式，发现可以，但是在Medium中没法使用很多种标签，但是最后通过修改name的值完成留言板功能的存储型XSS注入，因为前端对name长度限制，因此需要使用burp修改name参数___。

### 二、关键受阻点
1.  **POC脚本工程化受阻**：
    - 我已清晰的逻辑：___先发送请求并对指定的参数进行设定，记录返回值并比对，查看是否in 返回值.txt，如果存在，则进行不同payload列表测试___（例如：先GET取token，再POST带Cookie和payload）。
    - 我卡住的具体语法/报错：`___request库中面对的很多函数不知道内部形式是什么样子，比如url是否应该带""___`。
    - 我已查明的解决方案：需要学习___后续cookie和token获取的具体操作，已经对剩余语法内容的查询___（例如：Python中如何解析HTML获取表单token）。

### 三、侦察数据
- **心流强度**：今日在___13___时最为专注。
- **挫败感性质**：POC脚本的挫折属于 **建设性痛苦**（明确知道缺什么） / **耗散性痛苦**（感觉混乱）。
- **协议适配信号**：
  - [x] 我对绕过防护、探索不同漏洞变种感到持续兴奋。
  - [x] 我对编写工具实现自动化有强烈意愿（尽管暂时受阻）。
  - [x] 我渴望进入更底层/更贴近系统的领域。

### 四、系统状态审计
- **结束状态**：精神值枯竭（思维粘连、头晕）。
- **今日反思**：精力消耗在___闲杂事务，处理外部关系，和面对外部关系的开环消耗___，未来可通过___避免陷入深层交流，即使抽离___优化。
- **明日启动预警**：需执行低功耗恢复协议，优先解决POC语法问题。

## 周报 · web · D2 数据

🧠 **认知卡：今天到底明白了什么？**

- 核心发现：脚本写不下去，不是Python语法问题，而是 “网络协议”理解不透。

  - 旧想法：缺代码积木 → 学语法

  - 新认知：缺协议地图 → 懂HTTP各部件如何联动

- 关键收获（用你的话填空）：

  - 写POC更像 “______” （如：给机器人写手术指南），必须先懂人体结构（协议）。

  - 靶场和真实网络的差距，就像 “______” （如：实验室与街头实战）。

**⚙️ 工程卡：今天到底做到了哪？**
	- 今日目标：不写完整POC，只建可靠的基础连接（能登录DVWA并访问XSS页面）。

- 当前进度快照：

  - 已打通环节：______ （例：已能发送GET请求）

  - 卡住的具体问题：______ （例：不知道如何自动从页面获取token）

  - 下一步（明天首个15分钟的任务）：______ （例：搜索“Python解析HTML获取input值”）

- 完成度直觉打分（0-10分）：

  - 协议清晰度：__分

  - 代码实现度：__分

  - 障碍明确度：__分

📈 **状态卡：精力与明日规划**

- 中止信号：我在 ______ （例：感到一次轻微头晕）时，果断停止了。

- 明日三步走：

  - 扫尾（≤60分钟）：集中攻克上面写的“卡住的具体问题”。

  - 闭环（目标）：实现 “脚本可靠登录并跳转到目标页面”。

  - 新篇（随后）：基于此可靠连接，规划下一步（发送载荷、检测结果）。

